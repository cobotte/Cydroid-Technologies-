# Public assets directory for CyDroid Technologies
#
# Place static files here that will be served from the root URL:
#   - /assets/og-default.png  → Open Graph default image (1200×630)
#   - /assets/logo.png         → Organization logo
#   - /assets/favicon/         → Favicon set (multiple sizes)
#   - /assets/fonts/           → Self-hosted font files (Phase 2)
#   - /assets/videos/          → Optimized video files (Phase 3)
#   - /assets/icons/           → SVG icon library (Phase 2)
#
# Files in /public/assets/ are served as-is — no Next.js processing.
# For images that require optimization (resizing, format conversion),
# use next/image and reference them from /public/assets/.
